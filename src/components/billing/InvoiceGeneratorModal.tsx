/**
 * InvoiceGeneratorModal - Legacy import for backwards compatibility
 * This file now imports the refactored version from the InvoiceGenerator directory
 * 
 * The component has been refactored for better performance with:
 * - Smaller, focused components
 * - Custom hook for state management
 * - Memoization to prevent unnecessary re-renders
 * - Debounced filter inputs
 * - Optimized calculations with useMemo
 */
export { default } from './InvoiceGenerator/InvoiceGeneratorModal';